#include <stdio.h>
/**
 * main - Entry point
 * The program prints _putchar, followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i = 0;
	char p[] = '_putchar';

	while <= 7;
	{
		putchar(p[i]);
	}
	putchar('\n');
	return (0);
}
